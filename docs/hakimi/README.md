# Hakimi integration handoff

Current delivery, 2026-09-10 06:38 UTC (supersedes older source-only/pending
installation paragraphs below): AITP 0.10.0 / adapter-contract-0.3 is installed
in Codex and Hakimi. Latest Hakimi CLI 0.21.0 core/worker/Web match the verified
package; restart followed a complete 225-session idle check. New synthetic
session fb8775d0… reports ready, Python 3.12, no Goal. Agent/nested/Swarm explicit
decision-dependency cases and compaction recovery have finite model evidence;
this is not production-science acceptance or an OS sandbox. Atomic Note save
and Entry/Note failure/retry/concurrency evidence are recorded in the atomic-save
tests and lean-harness-live-acceptance document. Codex needs a new thread to
load cache 0.10.0+codex.20260910054835. The finite G1–G7 coverage audit is complete;
explicit old/new Note history recall passed and final REST/WS tests passed 32/32.
Scientific task completion, universal recall and OS isolation are not claimed.
Earlier dated notes retain their observed failures and former authorization
boundaries; they do not override this installed-version statement.

Packaging audit (2026-09-10): both plugin manifests now describe implemented
Entry/Note memory and atomic scope saves without presenting graph/Insight roadmap
layers as shipped. Distribution/release/adapter tests: 16 passed; plugin manifest
validator passed using the repository environment. Daily compatible Hakimi core
and AITP 0.10.0 are installed. New synthetic session
session_1e0bf2f7-9b91-4b5a-95d9-8587ff17b17a reports ready, contract 0.3,
plugin 0.10.0, Python 3.12, and no Goal. This is adapter loading evidence,
not another real-model scientific acceptance run.

Natural-use Note follow-up: provisional plans may reference an attributed
proposal without claiming results. `using-aitp` now distinguishes no results
from no source refs and asks for a local prose/metadata consistency review on
draft repair. Recall reports explicit contradictions. This is Skill guidance,
not deterministic semantic validation; deployed-model acceptance is pending.

Hakimi source follow-up: human decisions optionally carry explicit dependent
Goal IDs in local state and host transport. Only named Goals are held; missing
legacy dependencies remain unknown and conservative. This does not mutate or
extend AITP human decision authority/schema. Legacy unknown dependencies and explicit
Goal IDs are displayed by Board/Manager and verified in isolated browser sessions.
Task-level dependency audit and final installed acceptance remain pending.

Atomic Note source update (2026-09-10): AITP 0.10.0 / contract-0.3 implements
paired expected-topic/exact-workstream Note save, with parse/validate/retry inside
the write lock. Contract/CLI/version surfaces agree; legacy no-flag saves and
file/read schemas unchanged. Hakimi source now recognizes 0.1/0.2/0.3,
retains atomic Entry support on 0.2/0.3 and forwards captured Note scope on 0.3.
Its service suite passes 711 tests, typecheck and import-boundary checks pass.
Cross-process Note scope save and retry have isolated CLI/adapter evidence.
Latest AITP ledger suite: 220 passed. Daily deployment remains pending, not installed support;
do not deliver contract-0.3 to a daily Hakimi adapter that does not recognize it.

New finite Goal, 2026-09-10: explicit authorization now covers minimal atomic
Note scope save and explicit human-decision dependencies in Hakimi. Ownership
and legacy migration table is in Hakimi's research-mode-lean-harness-plan.zh-CN.md;
Note contract and synthetic pre-lock failure evidence are in
../hakimi-note-atomic-save-spec.md. Runtime implementation is now present as
described above; final installation is pending. Older pending-authorization
entries below are historical.

Installation follow-up, 2026-09-10 02:35 UTC: theory/history guidance below is
now locally reinstalled on Codex (0.9.0+codex.20260910023532) and Hakimi (0.9.0).
All 65 source bundle files match both copies; public Hakimi install returned
enabled/ok with no diagnostics after 224 listed sessions were confirmed idle.
No daemon restart, scientific task or ledger write; new-session behavior untested.

2026-09-10 theory/history guidance (source-only, not reinstalled): using-aitp
adds on-demand theory/numerics examples and distinguishes record supersession
from refuting an old calculation. No compulsory report/Entry/Note/card chain.
The existing note-prepare description is synchronized; version 0.9.0 and
contract schema 0.2 stay unchanged. No host policy or runtime change, no new
model-behavior or speedup claim. Scope: ../research-memory.zh-CN.md#theory-history-refinement.

L3 source-only: optional GetResearchStatus line_slug projects an existing Line without changing execution focus, tasks or checkpoint ownership. No AITP/schema migration; Board and client browsing integration remains unfinished.

2026-09-09: a finite Hakimi Research Mode simplification/context-deduplication
Goal is active. The detailed L0–L7 plan is in Hakimi
`docs/aitp/research-mode-lean-harness-plan.zh-CN.md`. This is authorized work in
progress, not completed behavior; no AITP runtime/schema or human-authority
change is authorized by the plan. Existing Skill refinements remain source-only.

L1 source-only update: unchanged Research disclosures are deduplicated across
turns; Goal task reminders are separated from compact usage updates, and Research
does not repeat the Goal objective/criterion. Request-chain and recovery coverage
passes in 745 related Hakimi tests. This does not establish L2–L7 completion,
installation, or an AITP CLI/schema change.

L2 source-only slice: ordinary file/web observations and existing task status/output
queries no longer require a Research action/phase; the guard abstains, leaving
normal tool permissions intact. Canonical writes, Bash and unknown MCP tools do
not gain blanket observation privileges. A finite literal shell-query shortcut
now covers explicit job queries and log reads, optionally over SSH; composition
and submission are excluded, normal permissions still apply, and this is not OS
isolation. Live acceptance remains unfinished; no AITP CLI/schema change or
reinstall is claimed.

Distillation loading refinement (2026-09-09; source only, not reinstalled):
Follow-up pin guidance: ordinary recall adds no standalone per-file hash audit;
new pins may reuse verified same-target unchanged digests. Existence is not
version identity. Save/check, exact-card pins and historical verification remain
unchanged; no host permission or runtime change.
ordinary read-only recall no longer instructs loading full candidate review just
because historical markers exist. Current-task/touched-evidence relevance comes
first; explicit requests, relevant recurrence and new card trial/revision evidence
retain full review. Drafting/trial/human-decision rules, version 0.9.0, contract
schema 0.2 and CLI/runtime are unchanged; contract sync text reflects the routing.
No Hakimi runtime/permission change or behavioral speedup claim. Evidence and
limits: AITP feedback/2026-09-09-distillation-loading-natural-use.md.

Check-call deduplication (2026-09-09, reinstalled in Codex and Hakimi):
using-aitp now discovers and verifies a Python 3.11+ path before CLI use,
reuses current pre-write/closeout views only over a known-unchanged interval,
and uses one global check plus scoped enter for multi-line read-only recovery.
Evidence/ledger/policy changes, concurrent writers or uncertain freshness
require refresh; every save still gets check/enter verification. Scoped check
itself still scans globally under M1d; no new runtime/cache/schema/CLI or host
permission behavior. The added fixture verifies zero-write multi-line reads and
stale-report invalidation after evidence changes. This is Skill guidance, not
a scheduling hook or a measured model speedup.
Validation:193 ledger tests pass/44.74s, Skill validator and diff checks pass.
Single local GW call-pattern comparison:3checks+3enters5.440s versus
1globalcheck+3enters2.965s; equal enter views, no real-record writes. This is
not a controlled timing study or a new model-conformance claim.
Installation:2026-09-08 19:08UTC,65files match on both hosts. Hakimi enabled/ok,
no diagnostics,227sessions idle before install. Codex cache version
0.9.0+codex.20260908190759; use a new Codex thread for new Skill pickup.
No system-Python replacement, Goal resumption or scientific-record write.

Five-topic bounded memory acceptance completed (2026-09-09): five fresh mode-off
sessions passed all30frozen questions; Si postorganization retest passed7/7
(six repeated plus exact-version recovery). Earlier provider522 attempts are
unscored; no speedup or months-long retention guarantee. See
`../five-topic-memory-acceptance.zh-CN.md` for evidence and remaining limits.
Si public save added one recovery observation and one superseding
agent working Note, retaining old pins and unresolved scientific failures.
The on-demand memory reference and existing enter description distinguish old
version retrieval from record discovery and scientific validation; nested Git
locators are not root-repository validated pins. Contract0.2/version0.9.0 and
runtime/CLI/schema are unchanged. This increment is installed and cold-retested;
no Hakimi runtime, permission, Skill-visibility or orchestration change.
Validation: ledger192passed (41.93s), Skill validation passed. Installation was
attempted once after224sessions were verified idle; the API request timed out,
and a following read-only comparison still found the two intended differences.
Plugin-list inspection initially also timed out. Without resubmission,17:57UTC
Sep8 verification found all65files matching and plugin enabled/ok with noerrors;
the serialized install/reload had completed. All224sessions remained idle.

Read/write efficiency slice (2026-09-09 Asia/Shanghai; locally installed):
using-aitp entrypoint684->182lines; on-demand recording and CLI-contract
references preserve detailed rules. Existing enter/check/note-save descriptions
are synchronized, contract0.2/version0.9.0 unchanged. No runtime/schema/CLI/
Skill-visibility change. Final ledger191passed; final plugin reload16:14:48UTC
on Sep8 verified65files after all217sessions were idle. Independent reads can
batch; dependent writes and complete check reports/exit status remain protected.
Two fresh mode-off read tests reduced model requests12->8 and13->9, but Chain
needed an assisted2-request correction separating standing direction from paused
Goal execution; not an unassisted correctness or causal speedup claim.
Isolated synthetic save/retry/working-Note/scoped-query acceptance passed with
exactly1Entry/1Note, no duplicates or scope leakage. All974 real-store Markdown
files unchanged. Approval wait remains a major host/supervision cost; no write-
speedup claim. Detailed results: AITP docs/research-memory.zh-CN.md, efficiency
acceptance section; private Hakimi .tmp/aitp-lean-read-write-20260909 reports.

Authorized navigation application (2026-09-08): user confirmed the narrow
ordinary-file exception; GW Topic navigation now separates four explicit
directions with dated Note entrances. Original identity/Research Goal and all
other 973 canonical Markdown files across the three stores remain unchanged;
no records added. Whole-store check is byte-identical before/after (exit1,
73 errors/201 warnings), and scoped enter preserves each line's own Note.
This is not a new CLI command or human scientific/Goal-alignment decision.
Mode-off public-file/CLI recovery and no-op maintenance were tested; automatic
host Skill discovery remains unavailable. No runtime/schema/harness change,
new-template positive save, full-recall or efficiency claim. Earlier pending
permission/installation statements below are historical observations.

Topic-map/writing follow-up (2026-09-08; locally installed/reloaded at 14:46 UTC,
new real-session acceptance pending): on-demand guidance separates a stable Topic
map from scoped current records and pinned long-form derivations. Existing
theory-template/enter descriptions are synchronized; no runtime, CLI, schema,
authority or Hakimi harness change. A scoped regression covers old Topic links
and rejects prose-inferred membership. Topic-update and Note-show remain
unavailable; real GW Topic editing awaits resolution of canonical-write authority.
Earlier installation/test reports below describe earlier slices only.
This slice: 190 ledger tests passed (36.38s), Skill validation and scoped diff
checks passed. All 210 sessions were idle before installation; 63 installed
files match source. A fresh mode-off navigation test failed automatic entry
discovery: host Skill visibility requires active Research Mode, and bare aitp
is absent from PATH. With installed file/CLI locations supplied, the same
read-only session recovered distinct QSGW lines and evidence limits with one
check and no writes. This assisted result does not prove automatic discovery;
changing host Skill visibility is outside this docs-only Hakimi slice.
A second fresh mode-off test recovered the recorded phase-bound derivation and
its scientific limits. Given permission for justified synthesis, it chose no
write because the existing Note covers the evidence. Both sessions ended idle;
this does not exercise a new-template save. Real Topic update still needs the
explicit canonical-edit exception; its private navigation proposal preserves
the original identity and Research Goal. No completion or speedup claim.

Cold-recall repair follow-up (2026-09-08; locally reinstalled/reloaded):
the memory reference and enter description clarify that only active Entries
contribute `resolves`; a superseded resolver's successor does not inherit the
edge. Reuse current check reports and already inspected evidence within a
session without removing session/save verification. Same-line and cross-line
replacement regressions preserve the existing projection. No runtime, schema,
human-decision or Hakimi harness changes. NiO/Bi2Se3/Si synthesis repair is saved
and post-save verified; five fresh mode-off retests completed with mixed costs,
not an efficiency claim. Public prepare/save appended three same-scope Working
Notes while preserving all 971 old canonical files and existing findings.
All 189 ledger tests pass; 203 sessions were idle before reload and all 63
installed plugin files match source. No server restart or CLI rebuild needed.
Note-save guidance also leaves validation to the public CLI, not an ad hoc
draft parser; temporary supervisor/save waits are not durable research actions.
A fresh targeted Heisenberg test recovered the resolver/successor distinction
without supplied record IDs or answer. The initial Si retest omitted a current
configuration limitation; its synthesis was repaired and a further independent
Si retest recovered it plus dataset/status boundaries. See the companion
research-memory guide §9 for timing and coverage limits. Final suite189/35.58s.

Research-memory read/write guidance (2026-09-08; locally reinstalled/reloaded):
AITP's installable `using-aitp` now links an on-demand
`references/research-memory.md`; existing working/theory Note template prompts
emphasize questions, evidence-to-claim explanations, changed understanding, and
proof boundaries. `aitp.contract.json` updates the existing enter/note-save
descriptions; schema remains `aitp/adapter-contract-0.2`, version metadata 0.9.0.
No flags, file/read transports, runtime logic, frozen closeout-first ordering,
Method-card rule or human authority changes. Consumers must review the changed
contract bytes; source edits alone do not update an installed copy or live
session. This delivery used the existing plugin install/reload API, with all
198 listed sessions idle; 62 installed bundle files matched the reviewed source,
and the original Si session read the new reference. Do not add a per-action health check, automatic scope inference,
or a second copy of this memory guidance in Hakimi. Note save still lacks the
Entry-only atomic exact-scope variant. Three synthetic, real-use-inspired CLI
journeys cover scoped recovery with newer evidence, preserved human decisions,
and append-only synthesis without duplicate events; they are not live-model
conformance or scientific acceptance. See [user guide](../research-memory.zh-CN.md).
Validation: all 187 AITP ledger tests, using-aitp Skill validation, and both
repository diff checks pass. No Hakimi runtime/client rebuild or test suite was
needed for this plugin-only delivery; existing dirty changes remain intact.
The supervised Si turn recovered newer same-line Entries, updated its Question,
and saved one superseding Working Note. All 969 pre-existing canonical files
were unchanged; the other four observed sessions' scientific fields, all Goals,
bindings, decisions and committed histories were unchanged. No Entry was added.
This is partial acceptance: Skill-reference Read was rejected until an action
was started, Question/snapshot revisions were confused once, and the Note needs
exact citations of the Entries carrying the latest state observations, not just
submission receipts. Four relay failures prevented the final response; only the
test-owned active prompt and its unexecuted correction were cancelled. No Goal
was resumed and no remaining prompt/action/checkpoint was left by this test.
See [delivery findings](../research-memory.zh-CN.md#8-本地交付与真实会话复核2026-09-08).

Isolation acceptance follow-up (2026-09-07; tests only): two fresh-process CLI
cases interleave clients on one Topic, retaining A's pending draft while B saves,
rejecting a wrong/similar scope without canonical writes, and preserving scoped
handoffs and idempotent retries on A → B → A. All 183 ledger tests pass. Hakimi
adds independent-journal delayed-result/cold-replay isolation; 713 related tests,
typecheck and import checks pass. These are complementary transport/service
tests, not a simultaneous scheduler or full five-subject scientific gate. No
runtime, schema, Skill or authority change; no reinstall or real ledger mutation.

Hakimi scoped Note retrieval (2026-09-07; locally installed, cold reads verified):
a real Si review selected a NiO Note by Glob order; AITP scoped enter correctly
returned the Si Working Note. Hakimi's existing maintenance guidance now points
missing Note locators to one on-demand `aitp_enter` with the confirmed workstream
and `recent: 1`, followed by exact Read and workstream verification. No new health
cycle, write trigger, context provider or binding inference. 714 related Hakimi
tests and typecheck/import pass; a supervised corrective read found the proper
old Note, not an installed or autonomous-behavior guarantee. AITP runtime, CLI,
schemas, contract, Skills and canonical records remain unchanged.
CLI/Web byte equality, reproducible Web build and native PTY pass; only the
owned idle server was restarted. Five-session cold reads preserve scientific
fields, bindings, Goals and history, plus three checked canonical Entries. New
guidance appears in the real Si context; autonomous selection remains unproven.
In a separate explicitly requested synthesis, Hakimi saved a Si Working Note
through existing Note prepare/save, using existing pins and exact confirmed
workstream membership. Native post-turn maintenance
sees it and reports scoped clean; unresolved failures remain. All old canonical
Entry/Note bytes across three workspaces are unchanged, as are the other four
sessions' scientific fields. No new Entry/card/trial, experiment, binding or Goal
change. This is supervised Note-workflow acceptance, not autonomous distillation
or an extension of record-only atomic compare-and-save to Notes.

Later supervised Si source/algebra rebuttal saved one exact-scope result and
updated its Question without changing old canonical records or the other four
sessions' scientific fields. Synthetic checks support a diagnostic-design
correction, not reproduction or resolution of the Si failure. Native checkpoint
checks were clean, yet the model repeated enter/check before native turn-end
refresh. A subsequent Hakimi success-only checkpoint response now identifies
completed native show/scoped verification and asks the model to reuse its receipt;
needed evidence reads, Note checks, candidate checks and stale/external changes
remain separate. Failed/duplicate commits do not claim fresh verification. Seven
extended regressions pass after failing before the change; 714 related Hakimi
tests, typecheck/import and six unchanged AITP contract tests pass. No new I/O,
state, hook, card/trial, AITP runtime/Skill/contract or authority change. This
follow-up is locally installed: CLI/Web byte equality, reproducible Web, native
PTY and five-session cold-scope/history preservation pass. Only the owned idle
server was restarted. One original NiO read-only request completed with only
Begin → shell query → Read → Conclude and zero canonical writes; other subjects'
state remained unchanged. It had no durable commit, so non-repetition after the
new commit response remains unproven. Running status is not scientific acceptance.

Hakimi Focus disclosure (2026-09-07; locally installed, read-only model replay passed): the default
model status summary omits captured Focus boundedAction, distinguishing historical
selection intent from current effectiveNextStep. Original Focus remains in full
and public snapshots; no scientific-state repair, Goal/binding change or new AITP
operation. Four new regressions and 815 related Hakimi tests pass, along with
typecheck/import and read-only source projection of five real session snapshots.
CLI/Web byte equality, reproducible Web build, native PTY and five-session cold
reads pass. One real status-only prompt used only GetResearchStatus and answered
with the current diagnostic; scientific fields, bindings, Goal and history, plus
three checked canonical Entries, remained unchanged. This is one explicit read-only
answer, not autonomous research or full scientific acceptance. AITP runtime/CLI/
schema/contract/Skills, canonical records and human-decision semantics are unchanged.

Saved checkpoint key verification (2026-09-07; locally installed; cold read verified): Hakimi
compares canonical show.frontmatter.idempotency_key with the captured nonempty
checkpoint key before advancing its committed cursor. A missing, malformed or
changed key retains the pending record and receipt; no resave or replacement
is attempted. Exact retries remain idempotent. The same existing show call is
used by both verification paths, with no additional CLI call or AITP change.
Sixteen new regressions cover canonical mismatch, corrupted local key and valid
retry; 778 related Hakimi tests and six unchanged AITP contract tests pass.
This is post-save defense, not atomic pre-save key enforcement. The motivating
Si draft was corrected under supervision before saving; no wrong-key canonical
save was observed there. AITP 0.9.0/contract-0.2 and all public transports remain
unchanged. Five-subject and autonomous scientific acceptance remain incomplete.
Delivery: CLI 0.21.0 entry/worker/provenance and all 521 Web files match the
tested build; reproducible Web rebuild and installed PTY smoke pass. Only the
owned idle server was restarted. All five real sessions retained their Lines,
Questions, bindings, Goal status and commit histories; three latest canonical
Entries read through AITP show were unchanged. No model prompt or ledger save
was performed. This cold-read check is not an installed negative-key experiment
or evidence that the underlying scientific goals are complete.

Line-return focus (2026-09-07; locally installed and replayed): a settled
Line switch restores its last open/active/blocked Question only when the archived
period captured the same Topic observation. It uses the Question's current next
step, not old action state. Unknown/changed scope, closed/deferred/cancelled
Questions and a latest unfocused period stay unfocused. No AITP writes, binding
confirmation or Goal resumption; public snapshot shapes are unchanged.
Hakimi-only checkpointed period metadata and existing focus op; no AITP runtime,
CLI, Skill, contract, fixture or human-decision change. 762 focused Hakimi tests
and typecheck/import pass. A real NiO unchanged-job query no-opped at 03:58 CST.
Installed CLI/Web/PTY checks pass. Real Heisenberg A→B→A restored the original
Question; canonical Entry/Note bytes, Questions, Lines, commit history, explicit
binding and paused Goal remained unchanged. No parallel scientific Loop claim.

Hakimi model status disclosure, 2026-09-07 (locally installed and replayed): GetResearchStatus
adds optional summary/full detail, defaulting to summarized check receipts while
retaining scientific state and recovery identity. Full returns the unchanged
Research snapshot. No AITP runtime, CLI, Skill, contract, fixture or human-decision
change; no public Research transport change. 18 targeted Hakimi tests and
typecheck/import and six unchanged AITP adapter-contract tests pass. Same real
snapshot measured 234,426→45,577 characters. The installed model received a complete
summary with errors retained, then updated the current Question from the original
Entry without resaving, querying remotely or resuming the Goal. This supervised
follow-up explicitly requested synthesis, so automatic behavior remains unproven.
No terminal/U scientific evidence was added; output size is not quality evidence.

Hakimi non-agent creator correction, 2026-09-07 (locally installed and replayed):
AITP prepare_entry and record-prepare CLI default an omitted created_by to
agent:unknown, including tool/source/human authority. Hakimi's saved-checkpoint
comparison incorrectly expected that required field to be absent. It now matches
the exact existing default without changing prepare intent, authority, scope,
or AITP CLI/schema/contract. 15 Hakimi provenance/prepare tests and six unchanged
AITP adapter-contract tests pass. The original real NiO saved Entry/receipt was
accepted at 03:31 CST without another save, new Action, remote query, Goal
resumption or binding change. Independent canonical show and evidence checks
passed. No AITP runtime or Skill changes; no physical-U or terminal-run claim.

Hakimi post-record review ordering, 2026-09-07 (locally installed):
the existing exact-plugin handoff now assesses touched-evidence applicability
before optional harvesting. Ineligible evidence means no extra scans/checks or
Action for that review; actual harvesting still obeys the external Skill. Three
handoff tests and Hakimi typecheck/import checks pass. CLI/Web byte equality,
reproducible Web build and native PTY pass. One supervised NiO recovery no-opped
without harvesting or extra checks; this does not isolate a causal effect or
prove general conformance. Remaining Hakimi issues: oversized model status
(198,063 characters) and pre-observation Question synthesis despite committed
persistence. They are not reasons to alter AITP records or decision semantics.
No AITP runtime, CLI, Skill, contract, fixture, stage or authority change.
The real paused-Goal theory audit now completed scoped result persistence and
Question update without resuming its Goal; full scientific acceptance is separate.

Hakimi paused Goal reminder, 2026-09-07 (locally installed): bounded user questions,
status checks and recovery do not authorize autonomous Goal resumption.
The existing prompt is corrected after a real theory-audit request resumed
unexpectedly; 22 Goal-injection tests pass. This is guidance, not a semantic
runtime guard. CLI/Web/PTY delivery checks pass and a controlled new-process
real-model retry kept the Goal paused through original observation save/commit
and a fresh derivation Action. That audit has now completed scoped result
persistence and Question update without resuming the Goal. No AITP CLI, Skill,
runtime, schema, contract, fixture or human-decision change; dirty files preserved.

Hakimi Theory Physics 0.2.5 follow-up, 2026-09-07 (locally installed): existing-failure
diagnosis starts from recorded run/source/dataset locations, expanding only to
answer the current uncertainty and naming a missing observable when inconclusive.
Motivated by actual historical over-search and a guessed missing path under 0.2.4;
no speedup or autonomous acceptance claim. No AITP Skill/runtime/CLI/schema/
contract/fixture or human-decision change. The original 0.2.4 request ended;
installation used the existing plugin API with all three owned loaded sessions
idle. Six managed files match source; no CLI rebuild or server restart.
The subsequent supervised Si correction added a source snapshot and corrected
the stderr-capture claim through one scoped observation/commit/Question update;
54 source lines independently matched. Old records/failures remained intact and
distillation no-opped. Draft placeholder/key repair and about15m20s latency keep
autonomous efficiency unproven. A separate NiO query at04:52 CST reported RUNNING
without U and completed in about79s with no ledger writes or additional scans;
the two sessions retained distinct workstream histories. Eighteen targeted Hakimi
late-result/save/cold-restore isolation regressions pass in1.59s. No AITP surface
change, five-subject completion or scientific success follows from these checks.
Skill validation, one Hakimi plugin-consumption test and six unchanged AITP
adapter-contract tests pass; these are not live-model behavioral acceptance.

Hakimi Theory Physics 0.2.4 guidance, 2026-09-07 (previous local delivery): its separate
domain pack clarifies receipt-only existing-job queries and bounded log
diagnosis after real repetitive MPI output. Plugin installation/discovery
tests and Skill validation pass; managed files matched that revision. Touched-Entry
applicability precedes optional harvesting; the external Skill still owns every
trigger and required check. Real behavioral acceptance remains pending.
This changes no AITP Skill, CLI, runtime, adapter contract, fixture,
stage or human-decision semantics. Existing AITP dirty changes are preserved.

Hakimi-only missing-Run guidance, 2026-09-07 (locally installed): a legacy submission
receipt is not a structured retained Run. An invalid observed_run_action_id now
receives explicit bounded-query guidance when no Run exists; the refusal and
scope/permission checks are unchanged. Five focused executor/recovery tests pass.
No AITP CLI, schema, contract, Skill, stage or authority change. Installed CLI/Web
bytes and native PTY pass; real scientific acceptance remains separate in Hakimi's
current handoff.

Hakimi-only prompt follow-up, 2026-09-07 (locally installed): its
pending-record evidence instruction now provides exact current checkpoint and
revision arguments, correcting a real-model stale-value attempt without weakening
the stale check or adding checks, hooks, tool permissions or AITP surface changes.
777 Hakimi service/ops/presenter tests pass; the controlled model attempt did not
reach canonical persistence or scientific acceptance. Both this prompt fix and
the phase-recovery amendment below are now locally installed and verified against
the build. The new real-model retry subsequently saved and committed original
Si submission Entry `entry-d20fae138e384d289709d4ecbfff14ac`, with exact singleton
`qsgw-headwing` membership, native clean scoped pre/post checks and independent
AITP `show` verification. Pending state cleared without a second human adoption
or Goal-alignment change. This is historical-submission persistence, not acceptance
of current numerical results; the broader real research goal remains incomplete.

Hakimi-only recovery amendment, 2026-09-07 (locally installed):
confirmed original local conclusions can enter the existing checkpoint flow
after an ownership decision returns to a non-executing planning/evaluation/idle
phase. The original concluded Action, Program/Line/Question/Plan freshness,
explicit binding, human decision and external-commit guards remain authoritative.
This adds no AITP operation, schema, contract, Skill rule or scientific authority;
AITP remains 0.9.0/adapter-contract-0.2. Hakimi service/ops pass 744 single-worker
tests, plus type/import checks; unchanged AITP ledger tests pass 181. The installed
CLI passed byte/Web/PTY checks and restored a real session's original pending
checkpoint without a second acceptance. Real persistence/scientific acceptance
is separate; no Goal alignment, remote job or canonical record changed in that
cold-restore check.
This note is additive: all pre-existing working-tree changes are preserved.

Single entry point for the cross-repository handoff between AITP (protocol,
persistence, evidence authority) and Hakimi (agent orchestration, tool
invocation, interaction UX). This directory is the AITP-side baseline that a
Hakimi development session should read before building or changing its AITP
adapter, and that AITP sessions must keep current whenever a stage, CLI, or
schema status changes.

Hakimi-only amendment, 2026-08-30: its Research Board and model context now
distinguish the AITP Topic Research goal from the current Goal milestone. In
`auto` permission mode, routine in-scope Research actions use Hakimi's normal
tool permission policy instead of creating a second durable approval gate; a
legacy approval is resumed only when it is tied exactly to the current planned
action, with an explicit standing-auto resolution. Review and scientific
decision gates remain human-owned. On Hakimi's default v2 engine, `/goal
resume` now wakes the Goal driver directly rather than injecting a synthetic
User message; the legacy rollback keeps its compatibility path. This changes
no AITP CLI, schema, stage, method-card rule, or H5/H6b status.

Joint S5.1 amendment, 2026-09-01: AITP 0.9.0 adds the reviewed paired
`record save --expected-topic <topic> --exact-workstream <slug>` atomic
compare-and-save variant and publishes `aitp/adapter-contract-0.2`. Hakimi
supports both adapter-contract 0.1 and 0.2 for compatibility, but requires
0.2 for checkpoint-bound save and derives both preconditions from the captured
explicit Line-to-workstream binding. This closes the S5 canonical
write-isolation gap; post-save `show`/scoped `check` remain defense in depth.
At S5.1 closure it started neither S6 nor H6b and changed no file/read
transport schema, method-card rule, or human-decision meaning. The subsequent
Hakimi-only S6 status is recorded below. Frozen contract:
`docs/archive/hakimi-s5-1-atomic-record-save-spec.md`.

Hakimi-only S6 amendment, 2026-09-01: `ConcludeResearchAction` now requires one
main-agent durability assessment at the same transition that concludes the
action and records its sole progress boundary. `no_durable_delta` schedules no
S6 persistence or distillation I/O. `durable_delta` creates one additive,
checkpointed commit candidate that fixes the source action, progress timestamp,
existing Entry kind, authority, provenance class, and rationale, then routes it
through Hakimi's existing prepare/fill/save/show/scoped-check/commit barrier.
Identical retries return the same pending candidate; conflicting retries,
duplicate progress, provenance mismatch, and dependent formal closure while the
checkpoint is pending fail closed. Human assertions and decisions remain
separate human-attributed Entries. This changes no AITP runtime, CLI, file/read
schema, adapter contract, Method-card trigger, or human-decision meaning.

Hakimi-only S7 amendment, 2026-09-01: after the first successful commit of a
new Research checkpoint, a stateless Agent-scope handoff resolves the exact
`aitp-research-protocol:distilling-methods` Skill and returns one same-turn
review steer containing only the touched Entry and checkpoint identities.
Duplicate commits and missing, hidden, non-model-invocable, or failed Skill
loading are non-blocking no-ops. Before potentially reusable evidence is
filled, Hakimi guidance also requires relevant-card retrieval and delegates
the Entry's exact-card pin versus observation marker to the external Skill.
Hakimi does not parse markers, decide triggers, scan the ledger, create or
revise cards on its own, approve, or publish. This changes no AITP runtime,
CLI, schema, adapter contract, fixture, Method-card rule, or human-decision
meaning. It persists no scheduler/retry state and makes no exactly-once or
crash-recovery claim; H6b native coordination remains planned and unavailable.

Hakimi-only S8 amendment, 2026-09-01: the existing public Research snapshot
now carries an optional `hakimi/research-distillation-attention-0.1` receipt
for only the latest exact committed checkpoint/Entry. It distinguishes
`review_requested` from `handoff_unavailable`; a newer commit hides an older
receipt, and stale handoff completion cannot overwrite the newer commit
revision. This is observational only: it does not claim an AITP trigger,
Method card, trial, completed review, approval, or publication, and it adds no
public mutation, retry ledger, scheduler, scanner, or recovery. AITP runtime,
CLI, file/read schemas, adapter-contract 0.2, Skill semantics, fixtures, and
human decisions remain unchanged. H6b remains planned and unavailable.

Hakimi-only S9 validation amendment, 2026-09-01: Hakimi used the initialized
`/home/bhjia/physics/GW_librpa` workspace for one bounded, read-only
revalidation of the real ABACUS job-1097 packet. Nine local manifest members
and all 313 classification rows reproduced the saved summary with zero
blockers. A wrong expected source checkpoint failed closed with no output; the
correct run created deterministic evidence and the identical retry was a
no-delta no-op. Through the reviewed 0.9.0 compare-and-save variant, exact
Topic `gw-librpa` and exact workstream `hakimi-s9-abacus-union-audit` saved
`entry-a071eb42792548f685520d4492615a63`; scoped pre/post checks were clean and
an identical save returned `already_saved`, while 71 errors and 201 warnings
remained explicitly outside scope. One low-trust method observation was
reviewed under the external Skill and correctly produced no card because no
trigger existed. A real-derived contract fixture preserved the exact
revision/binding/checkpoint/Entry and observational handoff across REST,
WebSocket, Node SDK, klient, TUI, and Web. This validation changes no AITP
runtime, CLI, file/read schema, adapter-contract 0.2, Skill rule, or human
decision. The current Hakimi maintenance snapshot does not separately expose
`check-report-0.2.counts.outside_scope`; that additive public-schema option is
`planned / unavailable` for a separate reviewed goal. H6b remains planned and
unavailable, and the combined ABACUS physical contract remains `UNVALIDATED`.

Hakimi-only S10 closure amendment, 2026-09-01: the finite Unified Research
Mode S0-S10 Program is closed after rerunning the full relevant matrix. Agent
Core passed 5,839 tests with one skip, TUI 3,152 with two skips, Web 914,
protocol 577, and the unchanged AITP suite 181. All seven affected-package
typechecks and builds, import boundaries, generated manifests, reproducible Web
assets, CLI/help/version, adapter contract, runtime budget, and both diff checks
passed. Full kap-server, klient, and Node SDK attempts reproduced only known
non-Research workspace-Skill, prompt-abort, MCP-trust, and open-handle baseline
failures; those remain non-green. The six Program changesets classify the CLI
delta as patch and aggregate the SDK delta to major; the repository-wide CLI
aggregate remains minor because unrelated committed-baseline changesets are
pending, and no versioning command ran. S9 did not exercise live crash or
cold-resume recovery, so it supplies no basis for an exactly-once claim or a
new coordinator contract. H6b and the public `counts.outside_scope` projection
remain planned and unavailable. This closure changes no AITP runtime, CLI,
schema, adapter contract, fixture, Skill rule, or human-decision meaning.

- Historical runtime audit: **2026-08-08**, AITP HEAD
  `8658f6827288f4bb61e5c193a346f0f73ebbe3b2`, read-only (only `/tmp`
  throwaway copies were written; no runtime, spec, or record was changed).
  This is historical audit evidence, not the current stage status. The
  current amendments are the 2026-08-10 M1a deterministic-gate completion,
  the 2026-08-12 M1b planning sync, the 2026-08-12 M1b-R1 gate completion,
  the 2026-08-13 M1c gate completion, the 2026-08-14 M1d gate completion,
  the 2026-08-15 0.6.0 Skill-only amendment, the 2026-08-15 M1e runtime slice, and the 2026-08-21 0.8.0 Skill-only amendment below.
- Current machine-readable adapter surface: `plugins/aitp-research-protocol/aitp.contract.json` (`aitp/adapter-contract-0.2`; 0.1 is the legacy 0.8 surface) is the executable projection of the command/schema decisions in `compatibility-matrix.md` and is shared by Hakimi and external harness adapters. Any CLI, transport-schema, model-facing description, or Skill-surface change updates it in the same change; `tests/ledger/test_adapter_contract.py` enforces consistency.
- Current amendment: the approved 2026-08-10 narrowed review closes M0.6;
  M1a is now **done; deterministic gate passed**. The 2026-08-09 no-turn
  preflight verified FROZEN v6 preparation and stopped before S3; it is not
  scored evidence. The original bootstrap Notes/decisions,
  recall/false-import/human-time, held-out S3, paired S1/S2, cold-start,
  conformance, causal, and treatment-advantage evidence remains not measured;
  deferred; not counted. M1a now exposes the versioned read contracts; see
  [`docs/archive/m1a-stage-notes.md`](../archive/m1a-stage-notes.md),
  [`docs/archive/m0.6-stage-notes.md`](../archive/m0.6-stage-notes.md), and the
  [review packet](../archive/m0.6-gate-review.md).
- 2026-08-12 M1b planning sync: the natural-use pause is complete and the
  reviewed freeze revision ([`docs/archive/m1b-adjudication.md`](../archive/m1b-adjudication.md))
  selected the read-side slice **M1b-R1**, implemented per its
  implementation spec
  [`docs/archive/m1b-r1-spec.md`](../archive/m1b-r1-spec.md).
- 2026-08-12 M1b-R1 gate completion: the R1 deterministic gate **passed**
  (evidence in
  `docs/archive/m1b-r1-stage-notes.md`). Hakimi may now feature-detect
  `aitp/check-report-0.1` (parsing the
  report on exits 0 and 1; exit 2 is the AITPError-driven JSON error
  envelope — argparse misuse (an unknown/repeated flag) exits 2 with
  stderr usage only, no JSON envelope). `lineage` is a
  deferred candidate.
  `enter` stays at `aitp/enter-0.2`; its text rendering is now
  compact, and that text is human-facing only — never feature-detected.
- 2026-08-13 M1c sync: **M1c (Topic workstreams) done; deterministic gate
  passed** per the frozen implementation spec
  `docs/archive/m1c-workstreams-spec.md` (gate evidence in
  `docs/m1c-stage-notes.md`). Optional explicit `workstreams`
  membership (unscoped legacy visible only in the global view), a repeatable
  `--workstream` prepare flag (duplicates rejected), and single-slug scoped
  read projections: with the single-occurrence `--workstream <slug>`, `enter`
  emits `aitp/enter-0.3` and `list` emits `aitp/list-0.2` (old payload plus
  one additive top-level singular `workstream` key; strict exact membership
  — unscoped records are excluded; relations computed on the whole store
  first, then strictly scoped projections including handoff; warnings stay
  global, and `check` stays global in M1c (pre-M1d — M1d supersedes the
  M1c no-scope-flag rule for `check`'s flag variant only; see the
  2026-08-14 sync below). **Without the flag the
  payloads stay `aitp/enter-0.2`/`aitp/list-0.1`, byte-unchanged.** No
  registry; `show`/`check` contracts unchanged in M1c (pre-M1d — the scoped
  `check` flag variant is M1d, superseding this for the flag variant only;
  see the 2026-08-14 sync below). Hakimi may now feature-detect
  and integrate the scoped contracts (H3).
- 2026-08-14 M1d sync: **M1d (Workstream health) done; deterministic gate
  passed** per the frozen implementation spec
  `docs/archive/m1d-workstream-health-spec.md` (gate evidence in
  `docs/m1d-stage-notes.md`). `check` gains a **single-occurrence
  `--workstream <slug>`** flag (a repeated flag is parser-rejected misuse)
  emitting the scoped transport **`aitp/check-report-0.2`**; **without the
  flag every `check` surface stays byte-identical `aitp/check-report-0.1`**
  (JSON, text, exit 0/1/2, zero-write). Scoped `counts` add the per-level
  `by_code` tally and the derived `outside_scope` level delta (global −
  scoped); scoped `counts.entries`/`counts.notes` are **admitted in-scope**
  counts — **not directly comparable** with `aitp/check-report-0.1`; exit
  0/1 are evaluated on the scoped report, so a scoped `clean` claims only
  "no attributable findings for this workstream", never whole-store health
  (`outside_scope` and the no-flag run carry the remainder); a well-formed
  slug with no admitted in-scope records is a valid empty scope. The scoped
  text is **exactly four human-only lines** — never parse or feature-detect
  it. Hakimi may now integrate the scoped contract (H4).
- 2026-08-15 0.6.0 Skill-only amendment: **0.6.0 is a Skill-only release,
  not a stage** — automatic session-boundary current-state maintenance
  (`using-aitp` Skill) and local method-card distillation
  (`distilling-methods` Skill). It changes **no** CLI command, flag, file
  schema, transport schema, exit code, or zero-write property; CLI/schema
  feature detection and every red line in `compatibility-matrix.md` are
  completely unchanged. Hakimi may orchestrate the existing commands for
  session-boundary maintenance (enter/check at session start; scoped
  variants per H3/H4 for the worked line; closeout/working-Note
  prepare→fill→save at session end per the H0 flow) and may treat method
  cards as ordinary local theory-mode Notes (H1/H3 read contracts apply,
  `show`/`list` unchanged; a card is just a Note whose body first line is
  the marker `> method-card: <slug>`). Publication of a method card into a
  Skill is human-gated and happens inside the AITP protocol repository, not
  by Hakimi. Hakimi must not direct-write canonical files, auto-run
  `inventory`, auto-backfill `workstreams`, or auto-publish method cards.
  Manual reviewed backfill is available only through `aitp backfill workstreams`
  with a human decision pinning the mapping; no inference/auto-backfill exists.
- 2026-08-15 M1e runtime slice: **M1e (Evidence lifecycle + reviewed
  backfill) done; deterministic gate passed** per
  `docs/archive/m1e-evidence-lifecycle-backfill-spec.md` (gate evidence in
  `docs/m1e-stage-notes.md`). Runtime version 0.7.0. Hakimi may feature-detect
  the `aitp/backfill-0.1` success envelope and may read `sha256-once`/policy
  finding codes in the existing check transports; no check transport schema
  changed. `backfill` is dry-run by default and write-on-`--apply`; it must
  only be called with a human decision Entry that sha256-pins the mapping.
  (red lines 2 and 4 remain in force).
- 2026-08-21 0.8.0 Skill-only amendment: **0.8.0 is a Skill-only release,
  not a stage** — method-observation markers (`> method-observation: <slug>`
  on eligible durable Entries; low-trust candidate, not proof; runtime never
  validates), conservative candidate review, post-card exact trials, two-step
  human decisions (card `Approve`/`Defer`/`Reject` then separate `Publish
  now`/`Keep local`; both saved as independent human `decision` Entries by
  the main agent; main-agent-only; no hardcoded model/preset), the platform
  tool/card/Skill three-layer boundary (tool executes, card records, Skill
  routes; bare `host:path` never accepted as evidence; host Goal never
  auto-imported), and a best-effort fallback lifecycle (no runtime hook, no
  exactly-once; native host coordinator planned but not implemented). It
  changes **no** CLI command, flag, file schema, transport schema, exit
  code, or zero-write property; CLI/schema feature detection and every red
  line in `compatibility-matrix.md` are completely unchanged. Plugin version
  moves to 0.8.0 on all four surfaces. A future Hakimi native C6/H6 Feature
  would own session/turn checkpoint, deduplication, recovery, question
  interaction, and adapter state for method distillation — but is not
  implemented; until then the Skill fallback is independently usable. The
  design record is `docs/method-cards-and-distillation.md`.
- Future evaluation-harness integration: Hakimi may consume or host an adapter
  for the separate project, but AITP does not assign harness ownership to
  Hakimi. No H0, H1, or H2 capability depends on harness implementation; see
  the [external evaluation-harness contract](../external-evaluation-harness.md).
- Full compatibility matrix, assumptions check, and decisions:
  [`compatibility-matrix.md`](compatibility-matrix.md).

## Boundary (both sides agree; re-verify before relying)

- AITP = protocol, persistence, evidence authority. Interface is **CLI +
  files**; no SDK, API server, MCP server, daemon, or vector service.
- Hakimi = agent orchestration, tool invocation, web retrieval, PDF reading,
  reasoning, and private caches. Private caches are **never written back** to
  AITP.
- Hakimi never copies the AITP runtime/parser/validator, never writes
  `.aitp` canonical files (`entries/`, `notes/`, `TOPIC.md`, `STORE.toml`),
  and never bypasses `record/note prepare|save`.
- Near-term ABACUS/LibRPA compilation, input/output handling, HPC, and artifact
  runners belong to Hakimi or an external platform adapter; AITP retains
  ledger/evidence/method-card semantics. The repositories cooperate through
  CLI + files and the adapter contract, with no runtime migration.

## Phased plan (Hakimi side; AITP side is the roadmap gates)

M0.6→M1a authorization belongs to AITP: the approved 2026-08-10 narrowed
gate review flipped M1a to ready, and the post-review deterministic gate is
now passed. Hakimi may feature-detect the shipped read contracts; it adds no
M1b adjudication. The exhaustive A–H roster and freeze process are normative in
[`docs/m1b-spec.md` §0.1](../m1b-spec.md#01-authoritative-candidate-roster-and-current-dispositions).
The H2 summary is non-exhaustive: integrate only capabilities shipped by a
selected, reviewed slice; see the A–H roster for E quick-run if separately
selected and shipped. G is independent Skill-track work and H is dropped.
Selected prepare/save changes follow the versioned-envelope or same-change
adapter-revision rule in [`compatibility-matrix.md` §3](compatibility-matrix.md).

| Phase | AITP prerequisite | Hakimi work |
|---|---|---|
| H0 | now (no gate) | Launcher adapter (argv-only, Python ≥ 3.11 probe), strict shape validation of unversioned envelopes, capability detection from `--help`, `enter` lifecycle, prepare→fill→save flow, graceful degradation on `not_initialized`, tree-hash zero-write tests — **Hakimi: implemented-in-code** |
| H1 | M1a done; deterministic gate passed | Read-only feature detection and schema dispatch for `aitp/enter-0.2`, `aitp/list-0.1`, and `aitp/show-0.1`; closeout-first handoff; Note-age signal; generated-golden compatibility tests — **Hakimi: implemented-in-code**; plugin version `0.8.0` (2026-08-21 0.8.0 Skill-only amendment) |
| H2 | M1b-R1 selected 2026-08-12; implemented per `docs/archive/m1b-r1-spec.md`; deterministic gate passed 2026-08-12 | Integrate only capabilities actually shipped by the R1 gate: `aitp check` (parse `aitp/check-report-0.1` on exits 0 and 1; exit 2 is the AITPError-driven JSON error envelope — argparse misuse (an unknown/repeated flag) exits 2 with stderr usage only, no JSON envelope), and consume the compact `enter` text only as human-facing output (never parse it; machine output is the versioned JSON). Persisted `based_on`/`used_by`, pointer bundles, quick-run, and `lineage` are **not** in R1 and must not be scheduled for H2. The M1d scoped `check` variant (`aitp/check-report-0.2`, single-occurrence `--workstream`) is H4 scope — **Hakimi: implemented-in-code** |
| H3 | M1c done; deterministic gate passed 2026-08-13 (evidence in `docs/m1c-stage-notes.md`; frozen spec `docs/archive/m1c-workstreams-spec.md`) | Integrate the scoped read contracts **only when passing the single-occurrence `--workstream <slug>`**: feature-detect `aitp/enter-0.3`/`aitp/list-0.2` (old payload plus one additive top-level singular `workstream` key), scope strictly by exact membership (unscoped records are **not** in scope), and keep warnings/malformed global; relations are computed on the whole store first, then projections including the handoff are strictly scoped. Without the flag, keep consuming `aitp/enter-0.2`/`aitp/list-0.1`; never parse the scoped `workstream:` text line. The repeatable `--workstream` prepare flag seeds draft frontmatter only (duplicates rejected) — prepare/save envelopes are unchanged — **Hakimi: implemented-in-code** |
| H4 | M1d done; deterministic gate passed 2026-08-14 (evidence in `docs/m1d-stage-notes.md`; frozen spec `docs/archive/m1d-workstream-health-spec.md`) | Integrate the scoped `check` contract **only when passing the single-occurrence `--workstream <slug>`**: feature-detect `aitp/check-report-0.2` (complete 0.1 payload plus additive top-level singular `workstream`, `counts.by_code` per-level tally, `counts.outside_scope` global−scoped level delta). Attribution is strict exact membership on **admitted** records only (parse + structure passed, ID unique) — unscoped, out-of-scope, malformed, duplicate-ID, and TOPIC.md findings are never scoped and surface only via `outside_scope`; relations are validated on the whole store first, then the globally sorted findings are restricted by scoped path (subset invariant). Scoped `counts.entries`/`counts.notes` are **admitted in-scope** counts, **not comparable across `aitp/check-report-0.1`/`0.2`**; exit 0/1 are evaluated on the scoped report (a scoped `clean` is not whole-store health), exit 2 unchanged; the four-line scoped text is human-facing only — never parse it. Without the flag keep consuming `aitp/check-report-0.1`, byte-unchanged — **Hakimi: implemented-in-code** |
| H5 | M1e done; deterministic gate passed 2026-08-15 (evidence in `docs/m1e-stage-notes.md`; frozen spec `docs/archive/m1e-evidence-lifecycle-backfill-spec.md`) | Hakimi projects check finding codes only; it does not expose, call, or parse the `aitp/backfill-0.1` success envelope and does not consume `sha256-once:` or check-policy semantics — **Hakimi: partial** |
| H5.1 / Unified Research S5 strong write gate | AITP 0.9.0 reviewed integration slice; frozen spec `docs/archive/hakimi-s5-1-atomic-record-save-spec.md`; gate evidence `docs/hakimi-s5-1-stage-notes.md` | Checkpoint-bound `record save` passes captured Topic plus exact singleton workstream through adapter-contract 0.2; AITP compares both under its write lock before canonical persistence; failed preconditions leave no canonical Entry; no-flag save stays compatible — **implemented; deterministic gate passed** |
| H5.2 / Unified Research S6 durable conclude | no AITP change; consumes the existing 0.9.0 adapter-contract-0.2 save and read barriers | Conclude records one explicit no-delta or durable-delta assessment; positive evidence becomes one checkpointed, provenance-typed candidate and immediately follows the existing prepare/fill/save/show/scoped-check/commit route; retry, duplicate-progress, pending-closure, undo, restore, and degraded paths fail closed — **implemented; release gate closed after explicit SDK-major / CLI-patch authorization and a validated Hakimi changeset** |
| H5.3 / Unified Research S7 bounded distillation handoff | no AITP change; invokes the existing external `distilling-methods` Skill | The first successful checkpoint commit schedules one same-turn best-effort review for only the touched Entry; duplicates and unavailable Skill paths no-op; all marker/card/trial/trigger/human-gate semantics remain in the Skill — **implemented in Hakimi; not H6b and not exactly once** |
| H6b / C6 | **planned; unavailable** — 0.8.0 Skill-only amendment defines the rule surface; future native method-distillation coordinator | A future Hakimi native Feature (`packages/agent-core-v2/src/features/aitp/`, not yet created) would own session/turn checkpoint, deduplication, recovery, question interaction, and adapter state for method distillation — bounded checkpoints at session start/resume, after adapter-save, turn-end/idle, state-change, dispose, and crash/cold-resume. Requires a reviewed adapter-contract extension (preferred: new schema version) before implementation. The 0.8 Skill fallback is independently usable until then. Detailed design is planned for a separate `docs/hakimi/method-distillation-orchestration.md` (not yet created) |
| Formal Hakimi contract | after M4 | **planned and blocked**: versioned `--json` + extended golden fixtures as the pass gate for any agent integration |

Hakimi's research-loop capabilities (web, PDF, reasoning, session UX, private
caches) are independent of all AITP gates and can proceed in parallel at any
time.

## Maintenance contract (binding)

Update this directory **in the same change** as any of the following:

- stage status flips (M0.6 gate, M1a gate, M1b gate, M1c/M1d slice gates,
  M2–M4);
- CLI surface change (new or removed command/flag; `--help` output);
- schema status change (new frozen payload/file schema, version bump);
- M1b scope or gate sequencing change (natural-use review, candidate
  disposition, selected slice, split/revision/deferral, or no-runtime result);
- a Hakimi-side integration finding that changes a matrix row or a red line.

The flip of a roadmap row happens through the gate review; this directory only
records the consequence. Never edit `docs/roadmap.md` stage statuses from
here. M1a has landed; the synchronized version metadata is `0.9.0`
(2026-09-01 Hakimi S5.1 atomic-save slice; 0.8.0 was the preceding Skill-only amendment; 0.7.0 was the preceding M1e runtime
slice; 0.6.0 was the preceding Skill-only release — no
CLI or schema change) and
the read contracts are available. Keep the frozen `suite/adapters/cli.md`
unchanged until a separately reviewed suite refreeze. Hakimi H1 may now
feature-detect the three versioned read schemas; `check` is shipped and
gated (M1b-R1 per `docs/archive/m1b-r1-spec.md`; gate evidence in
`docs/archive/m1b-r1-stage-notes.md`) and may be feature-detected now;
`lineage`
is a deferred candidate. M1c (Topic workstreams) is **done; deterministic
gate passed** (2026-08-13) per `docs/archive/m1c-workstreams-spec.md`; the
gate evidence is in `docs/m1c-stage-notes.md`, and H3 may now integrate the
scoped contracts. **M1d (Workstream health) is done; deterministic gate
passed (2026-08-14)** per `docs/archive/m1d-workstream-health-spec.md`
(gate evidence in `docs/m1d-stage-notes.md`) and M1e (evidence lifecycle + reviewed backfill, `docs/m1e-stage-notes.md`): `check` now exposes two read
transports — `aitp/check-report-0.1` no-flag (byte-unchanged) and
`aitp/check-report-0.2` with the single-occurrence `--workstream` — and H4
may now integrate the scoped contract. **0.6.0 (2026-08-15) is Skill-only**:
no new command, flag, or schema, so feature detection and red lines are
unchanged; Hakimi may orchestrate the existing commands for session-boundary
maintenance and treat method cards as local theory Notes (see the 2026-08-15
amendment above). The 2026-08-21 **0.8.0** Skill-only amendment
(method-observation markers, two-step human decisions, platform
tool/card/Skill boundary, best-effort fallback) moves plugin version to
0.8.0 with no CLI or schema change; the future H6b/C6 native coordinator is
planned and unavailable. **As of 2026-08-29, Research Mode is discoverable
by default, but a new session starts inactive.** The legacy
`KIMI_CODE_EXPERIMENTAL_AITP_RESEARCH_MODE`,
`[experimental].aitp_research_mode`, and `KIMI_CODE_EXPERIMENTAL_FLAG` master
flag are inert for this capability. H0–H4 are **implemented-in-code**: strict contract
discovery, Python probe, `enter`/`list`/`show`/`check` read-side consumption,
`record`/`note prepare|save` write-gated persistence, scoped `--workstream`
reads/checks, and a TUI `/research` command with Research Board and manager.
H5 is **partial**: Hakimi projects check finding codes only and does not
expose, call, or parse `backfill-0.1`, or consume `sha256-once:` or
check-policy semantics. Hakimi does not auto-run `init`, `init --adopt`,
`inventory`, or `backfill --apply`; `backfill` is not exposed as a model tool.
A managed AITP 0.8.0 CLI live smoke completed on a scratch store; the 0.9.0
atomic save variant has deterministic local contract tests, while full
cross-platform exceptional-case conformance remains pending. Typed
question/line registry, literature/compute/Portfolio support, and H6b native
   method-distillation orchestration remain **planned; unavailable**. Hakimi S6
   durable-conclude orchestration is implemented and release-adjudicated without
   an AITP runtime/contract change. Hakimi S7 adds only the bounded, stateless,
   same-turn external-Skill handoff described above and does not authorize H6b.

## Reading order

1. `/home/bhjia/physics/repo/AITP-Research-Protocol/AGENTS.md`
2. `/home/bhjia/physics/repo/AITP-Research-Protocol/README.md`
3. `/home/bhjia/physics/repo/AITP-Research-Protocol/docs/roadmap.md` (stage table, M1a, M1b, Hakimi contract)
4. `compatibility-matrix.md` (this directory)
5. `docs/archive/m1a-spec.md`, `docs/m1b-spec.md`, `docs/archive/collaborator-design.md`
6. The installed plugin's `skills/using-aitp/SKILL.md` (python probe order, command map)
7. The runtime: `plugins/aitp-research-protocol/scripts/aitp.py` + `scripts/vendor/aitp/`

Then the Hakimi repository's own `AGENTS.md`/`README.md`/architecture.
# Native read routing follow-up (2026-09-09; source only)

The Hakimi plugin Skill wrapper prefers available native `aitp_enter/list/show/check`
tools and avoids duplicate shell invocations. CLI fallback resolves from the
loaded Skill directory; denied operations must not be retried through Bash.
Contract descriptions are synchronized; schema 0.2 and CLI/write semantics are
unchanged. New-session behavior and installation remain pending.
